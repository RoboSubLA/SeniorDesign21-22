import rospy
import time

def main():
    rospy.init_node('tester', anonymous=True)
    pub = rospy.Publisher("cv_pub", test, queue_size=1)

    mainLoop:
        if result == 'gate':
            data.gateseen = true
        else:
            data.gateseen = false
            data.gatedis = -99
            data.gatediscentr = -99

        publish(data)


if __name__ == '__main__':
    main()
    # make pub
    # wait for data
    # if vaild return true
    # else false



def addtwo(a=2, b=3):
    return a+b


addtwo(b=7)